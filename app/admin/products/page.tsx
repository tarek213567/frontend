import { ArrowLeft, ImageIcon, Pencil, Plus, Trash2 } from "lucide-react";
import Image from "next/image";
import Link from "next/link";
import AdminShell from "@/components/AdminShell";
import { getProducts } from "@/services/productService";
import { removeProduct } from "./actions";

const formatPrice = (price: number) => `৳ ${price.toLocaleString("en-BD")}`;

export default async function AdminProductsPage() {
  const products = await getProducts();
  return <AdminShell title="Products"><div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><Link href="/admin/dashboard" className="mb-4 inline-flex items-center gap-2 text-xs font-bold text-[#52715a]"><ArrowLeft className="h-3.5 w-3.5" /> Dashboard</Link><p className="text-xs font-black uppercase tracking-[0.18em] text-[#ff6b00]">Catalog workspace</p><h2 className="mt-2 text-3xl font-black tracking-[-0.04em]">Products</h2><p className="mt-2 text-sm text-[#77907d]">Manage the products displayed across NexoBD.</p></div><Link href="/admin/products/add" className="inline-flex items-center justify-center gap-2 rounded-lg bg-[#00843d] px-4 py-3 text-sm font-bold text-white hover:bg-[#006f33]"><Plus className="h-4 w-4" /> Add product</Link></div><div className="mt-8 overflow-hidden rounded-2xl border border-[#e3ece4] bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-sm"><thead className="border-b border-[#edf2ed] bg-[#fbfdfb] text-xs uppercase tracking-[0.08em] text-[#77907d]"><tr><th className="px-5 py-4">Product</th><th className="px-5 py-4">Category</th><th className="px-5 py-4">Price</th><th className="px-5 py-4">Stock</th><th className="px-5 py-4">Status</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody className="divide-y divide-[#edf2ed]">{products.map((product) => <tr key={product.id} className="hover:bg-[#fbfdfb]"><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="relative flex h-12 w-12 items-center justify-center overflow-hidden rounded-lg bg-[#f5f8f4]">{product.image ? <Image src={product.image} alt="" fill sizes="48px" className="object-cover" /> : <ImageIcon className="h-5 w-5 text-[#8a9d8d]" />}</div><div><p className="font-bold">{product.name}</p><p className="mt-1 text-xs text-[#8a9d8d]">{product.brand}</p></div></div></td><td className="px-5 py-4 text-[#52715a]">{product.category}</td><td className="px-5 py-4 font-bold text-[#00843d]">{formatPrice(product.price)}</td><td className="px-5 py-4"><span className={product.stock < 10 ? "font-bold text-[#ff6b00]" : "text-[#52715a]"}>{product.stock}</span></td><td className="px-5 py-4"><span className={`rounded-full px-3 py-1 text-xs font-bold ${product.stock > 0 ? "bg-[#edf8ef] text-[#00843d]" : "bg-[#fbe9e9] text-[#b74848]"}`}>{product.stock > 0 ? "Active" : "Out of stock"}</span></td><td className="px-5 py-4"><div className="flex justify-end gap-2"><Link href={`/admin/products/edit/${product.id}`} aria-label={`Edit ${product.name}`} className="rounded-lg border border-[#dce8dc] p-2 text-[#52715a] hover:text-[#00843d]"><Pencil className="h-4 w-4" /></Link><form action={removeProduct}><input type="hidden" name="id" value={product.id} /><button aria-label={`Delete ${product.name}`} className="rounded-lg border border-[#f2d5ca] p-2 text-[#c45b35] hover:bg-[#fff5ed]"><Trash2 className="h-4 w-4" /></button></form></div></td></tr>)}</tbody></table></div></div></div></AdminShell>;
}
