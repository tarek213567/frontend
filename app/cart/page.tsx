"use client";

import { ArrowLeft, Minus, Plus, ShoppingBag, Trash2 } from "lucide-react";
import Image from "next/image";
import Link from "next/link";
import Footer from "@/components/Footer";
import Header from "@/components/Header";
import { useCart } from "@/context/CartContext";

const formatPrice = (price: number) => `৳ ${price.toLocaleString("en-BD")}`;
const standardDelivery = 60;

export default function CartPage() {
  const { items, subtotal, increaseQuantity, decreaseQuantity, removeFromCart } = useCart();
  const deliveryCharge = subtotal === 0 || subtotal >= 1000 ? 0 : standardDelivery;
  const grandTotal = subtotal + deliveryCharge;

  return <div className="min-h-screen bg-[#f7faf7] text-[#183c26]"><Header /><main className="mx-auto max-w-[1240px] px-4 py-8 sm:px-6 lg:px-8"><div className="mb-8"><p className="text-xs font-black uppercase tracking-[0.18em] text-[#ff6b00]">Your NexoBD basket</p><h1 className="mt-2 text-3xl font-black tracking-[-0.04em]">Shopping cart</h1></div>{items.length === 0 ? <div className="rounded-2xl border border-[#e3ece4] bg-white px-6 py-16 text-center shadow-sm"><ShoppingBag className="mx-auto h-10 w-10 text-[#00843d]" /><h2 className="mt-4 text-xl font-black">Your cart is waiting</h2><p className="mt-2 text-sm text-[#77907d]">Add something lovely from today&apos;s marketplace picks.</p><Link href="/#products" className="mt-6 inline-flex items-center gap-2 rounded-lg bg-[#00843d] px-5 py-3 text-sm font-bold text-white">Continue shopping <ArrowLeft className="h-4 w-4 rotate-180" /></Link></div> : <div className="grid gap-6 lg:grid-cols-[1fr_340px]"><section className="space-y-3">{items.map(({ product, quantity }) => <article key={product.id} className="flex gap-4 rounded-2xl border border-[#e3ece4] bg-white p-3 shadow-sm sm:p-4"><div className="relative h-24 w-24 shrink-0 overflow-hidden rounded-xl bg-[#f5f8f4] sm:h-32 sm:w-32"><Image src={product.image} alt={product.name} fill sizes="128px" className="object-cover" /></div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><div><p className="text-[11px] font-bold uppercase tracking-[0.08em] text-[#8a9d8d]">{product.brand}</p><h2 className="mt-1 text-sm font-bold sm:text-base">{product.name}</h2><p className="mt-1 text-sm text-[#52715a]">{formatPrice(product.price)} each</p></div><button aria-label={`Remove ${product.name}`} onClick={() => removeFromCart(product.id)} className="text-[#9aaa9c] hover:text-[#ff6b00]"><Trash2 className="h-4 w-4" /></button></div><div className="mt-4 flex items-center justify-between"><div className="flex items-center rounded-lg border border-[#dce8dc]"><button aria-label="Decrease quantity" onClick={() => decreaseQuantity(product.id)} className="p-2"><Minus className="h-3.5 w-3.5" /></button><span className="w-7 text-center text-sm font-bold">{quantity}</span><button aria-label="Increase quantity" onClick={() => increaseQuantity(product.id)} className="p-2"><Plus className="h-3.5 w-3.5" /></button></div><p className="font-black text-[#00843d]">{formatPrice(product.price * quantity)}</p></div></div></article>)}</section><aside className="h-fit rounded-2xl border border-[#e3ece4] bg-white p-6 shadow-sm"><h2 className="text-lg font-black">Order summary</h2><div className="mt-5 flex justify-between text-sm text-[#66806b]"><span>Subtotal</span><span>{formatPrice(subtotal)}</span></div><div className="mt-3 flex justify-between text-sm text-[#66806b]"><span>Delivery</span><span>{deliveryCharge ? formatPrice(deliveryCharge) : "Free"}</span></div><div className="my-4 border-t border-[#edf2ed]" /><div className="flex justify-between"><span className="font-bold">Grand total</span><span className="text-xl font-black text-[#00843d]">{formatPrice(grandTotal)}</span></div><Link href="/checkout" className="mt-6 block w-full rounded-lg bg-[#ff6b00] px-5 py-3 text-center text-sm font-bold text-white hover:bg-[#e85c00]">Proceed to checkout</Link><Link href="/#products" className="mt-4 flex items-center justify-center gap-2 text-xs font-bold text-[#00843d]"><ArrowLeft className="h-3.5 w-3.5" /> Continue shopping</Link></aside></div>}</main><Footer /></div>;
}
